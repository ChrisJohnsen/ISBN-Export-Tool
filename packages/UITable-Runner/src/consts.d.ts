declare module 'consts:dependencies' {
  const dependencies: {
    name: string | null,
    version: string | null,
    license: string | null,
    licenseText: string | null,
  }[];
  export default dependencies;
}
